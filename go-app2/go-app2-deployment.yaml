apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-app2-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-app2
  template:
    metadata:
      labels:
        app: go-app2
    spec:
      containers:
      - env:
        - name: APPNAME
          value: app2
        image: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        name: go-app
        ports:
        - containerPort: 3000
